🚚 Nexport 开发（截至 2026-08-15）
Progress: 已完成 22 个任务：FND-001、CLN-001～004、DATA-001～002、TEST-001、SEC-001～003、RBAC-001、SAFE-001、NOTIF-001、AUTH-001、QUOTE-001、ARTICLE-001、SEO-001、UI-001～004。UI-004 已把登录/注册/邮箱验证统一为账户访问检查点，补齐验证码移动宽度、泛化登录错误、live-region 与非空 Suspense 状态；`/dashboard` 和 `/user/**` 使用服务端登录门禁，Dashboard 只并行读取当前账户真实询价和通知，假订单/库存引用清零；用户询价、通知、资料、密码、偏好与登录历史统一为可恢复工作区，移除无行为头像上传和账户删除，并为通知删除增加确认；后台权限读取增加 10 秒超时与明确拒绝结果，sidebar 标记当前页，概览只读真实统计，文章/询价/用户/页面表格具备命名、键盘可聚焦的移动横向滚动区和定向空状态；通知广播采用两步确认，删除、发布及权限变更均需确认；后台站点设置只接受六个真实公开资料字段，示例公司事实和未接入运行时的安全参数不再展示或保存。已补齐 UI-004 单元边界与 Playwright 认证/真实 Dashboard/移动表格/删除确认用例；L0 静态边界、三语 JSON、diff whitespace 与 LICENSE 校验通过。
Plans: 下一任务为 HYG-001（依赖、死代码、旧资产和配置清理），随后依次完成 DOC-001、QA-001～003。
Problems: node_modules 与 Playwright 浏览器尚未安装；UI-002～004 的 typecheck、Vitest 与 Playwright 运行态验证因本地没有 `tsc`/`vitest`/`playwright` 可执行文件而未运行（本次 `pnpm exec tsc --noEmit` 与 UI-004 Vitest 均确认命令不存在），RBAC/SAFE/NOTIF/AUTH/QUOTE/ARTICLE/SEO/UI 新增的隔离 PostgreSQL 与 API/UI 用例，以及 Prisma、lint、typecheck 的 L1/L2 门禁按计划延后到 QA；react-markdown/remark-gfm 已登记到 package.json，pnpm-lock.yaml 仍待 QA-001 统一刷新。
